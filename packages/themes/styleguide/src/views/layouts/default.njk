{% import "macros/nav" as nav -%}
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">

  {% include "ui/stylesheets" %}

  <title>{% if page.title %}{{ page.title }} | {% endif %}{{ site.title }}</title>
</head>
<body>

{% block body %}

{% set components = state.components | collect %}
{% set pages = state.pages | collect %}

<div class="container">

  <nav class="nav">
    <div class="nav__section">
      <ul class="tree">
        <li>
          <a href="{{ route('index') }}">Overview</a>
        </li>
      </ul>
    </div>
    <div class="nav__section">
      <h4 class="nav__label">Docs</h4>
      {{ nav.tree('pages', pages | tree) }}
    </div>
    <div class="nav__section">
      <h4 class="nav__label">Components</h4>
      {{ nav.tree('components', components | tree) }}
    </div>
  </nav>

  <main class="content">
  {% block main %}{% endblock %}
  </main>

</div>
{% endblock %}

{% include "ui/scripts" %}
{% include "ui/reload" %}

</body>
</html>
