{% extends "layouts/default" %}

{% block main %}
<div class="page">
  <h1 class="page__title">{{ site.title | default('Project styleguide') }} </h1>
  <p>Using the <strong>{{ theme.name }}</strong> theme v{{ theme.version }}</p>
</div>
{% endblock %}
