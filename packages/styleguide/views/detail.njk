{% extends "layouts/default" %}

{% block body %}

<h1>Detail: {{ variant.handle }}</h1>

<p><a href="{{ variant.previewUrl }}">Preview &rarr;</a></p>

{% for panel in app.component.panels %}
<div class="panel">
  <h2>{{ panel.label }}</h2>
  {% render %}{{ panel.content | safe }}{% endrender %}
</div>
{% endfor %}

{# <h2>Files</h2>
<ul>
{% for file in component.files %}
  <li><a href="{{ file.url }}">{{ file.relative }}</a></li>
{% endfor %}
</ul>

<h2>Source</h2>
<code><pre>{{ api.getSourceString(component) | await }}</pre></code> #}
{% endblock %}
